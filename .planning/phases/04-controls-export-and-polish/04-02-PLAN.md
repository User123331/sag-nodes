---
phase: 04-controls-export-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/viewer/src/components/ControlPanel.tsx
  - packages/viewer/src/components/ControlPanel.css
  - packages/viewer/src/components/ShortcutOverlay.tsx
  - packages/viewer/src/components/ShortcutOverlay.css
  - packages/viewer/src/components/GraphCanvas.tsx
  - packages/viewer/src/components/GraphCanvas.css
  - packages/viewer/src/store/uiSlice.ts
  - packages/viewer/src/App.tsx
  - packages/viewer/src/index.css
autonomous: false
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
  - CTRL-05
  - CTRL-06
  - STAT-01
  - STAT-02
  - STAT-03
  - EXPT-01
  - EXPT-02
  - SHAR-02

must_haves:
  truths:
    - "Collapsible left sidebar with depth slider, node limit slider, provider toggles, layout toggle, export buttons, and reset button"
    - "Provider status dots show green/yellow/red next to each toggle"
    - "Graph canvas resizes to accommodate open/closed sidebar and detail panel"
    - "Radial layout arranges nodes in depth-based rings from center"
    - "Keyboard navigation moves focus between nodes spatially and by similarity score"
    - "Focus ring appears on keyboard-focused node; graph auto-pans to keep focused node visible"
    - "Shortcut overlay appears on ? key press"
    - "Export buttons download JSON and GEXF files"
    - "Reset button clears graph, controls, and URL hash"
  artifacts:
    - path: "packages/viewer/src/components/ControlPanel.tsx"
      provides: "Left sidebar with all controls"
      min_lines: 100
    - path: "packages/viewer/src/components/ControlPanel.css"
      provides: "Control panel styling"
      contains: ".control-panel"
    - path: "packages/viewer/src/components/ShortcutOverlay.tsx"
      provides: "Keyboard shortcut help modal"
      contains: "Keyboard Shortcuts"
    - path: "packages/viewer/src/components/ShortcutOverlay.css"
      provides: "Shortcut overlay styling"
      contains: ".shortcut-overlay"
  key_links:
    - from: "packages/viewer/src/components/ControlPanel.tsx"
      to: "packages/viewer/src/store/controlSlice.ts"
      via: "useGraphStore selectors for controlSlice state"
      pattern: "useGraphStore"
    - from: "packages/viewer/src/components/ControlPanel.tsx"
      to: "packages/viewer/src/utils/exportJson.ts"
      via: "import downloadJson, buildJsonExport"
      pattern: "downloadJson|buildJsonExport"
    - from: "packages/viewer/src/components/GraphCanvas.tsx"
      to: "packages/viewer/src/utils/providerFilter.ts"
      via: "filterByDepth, filterByProviders, filterByNodeLimit applied to graph data"
      pattern: "filterBy"
    - from: "packages/viewer/src/components/GraphCanvas.tsx"
      to: "packages/viewer/src/utils/keyboardNav.ts"
      via: "findNearestInDirection for arrow key navigation"
      pattern: "findNearestInDirection"
    - from: "packages/viewer/src/App.tsx"
      to: "packages/viewer/src/components/ControlPanel.tsx"
      via: "ControlPanel rendered in app layout"
      pattern: "<ControlPanel"
---

<objective>
Build the ControlPanel sidebar component, ShortcutOverlay, extend GraphCanvas with filtering/radial layout/keyboard navigation, and wire everything into the app.

Purpose: This is the main UI plan that delivers all visible control features. It consumes the tested utilities from Plan 01 and wires them into React components following the established DetailPanel pattern.

Output: Working control panel with sliders, toggles, export, layout toggle, keyboard navigation, shortcut overlay, and canvas integration.
</objective>

<execution_context>
@/Users/billyendson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billyendson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-controls-export-and-polish/04-CONTEXT.md
@.planning/phases/04-controls-export-and-polish/04-RESEARCH.md
@.planning/phases/04-controls-export-and-polish/04-UI-SPEC.md
@.planning/phases/04-controls-export-and-polish/04-01-SUMMARY.md

<interfaces>
<!-- Utilities created in Plan 01 that this plan consumes -->

From packages/viewer/src/utils/providerFilter.ts:
```typescript
export function filterByDepth(nodes: ForceNode[], links: ForceLink[], maxDepth: number): { visibleNodes: ForceNode[]; visibleLinks: ForceLink[] };
export function filterByProviders(nodes: ForceNode[], links: ForceLink[], enabledProviders: Set<ProviderId>): { visibleNodes: ForceNode[]; visibleLinks: ForceLink[] };
export function filterByNodeLimit(nodes: ForceNode[], links: ForceLink[], seedMbid: string, limit: number): { visibleNodes: ForceNode[]; visibleLinks: ForceLink[] };
```

From packages/viewer/src/utils/exportJson.ts:
```typescript
export function buildJsonExport(nodes, links, seedMbid, seedName, depth, enabledProviders): JsonExport;
export function downloadJson(data: object, filename: string): void;
```

From packages/viewer/src/utils/exportGexf.ts:
```typescript
export function buildGexf(nodes: ForceNode[], links: ForceLink[], seedName: string): string;
export function downloadGexf(xml: string, filename: string): void;
```

From packages/viewer/src/hooks/useUrlState.ts:
```typescript
export function useUrlState(): void;
```

From packages/viewer/src/utils/keyboardNav.ts:
```typescript
export function findNearestInDirection(focused: ForceNode, nodes: ForceNode[], direction: Direction): ForceNode | null;
```

From packages/viewer/src/store/controlSlice.ts:
```typescript
export interface ControlSlice {
  enabledProviders: Set<ProviderId>; providerStatus: Record<string, ...>; providerIsFetching: Record<string, boolean>;
  maxDepth: number; nodeLimit: number; layoutMode: 'force' | 'radial'; isSidebarExpanded: boolean;
  toggleProvider; setMaxDepth; setNodeLimit; setLayoutMode; setSidebarExpanded; resetControls;
}
```

From packages/viewer/src/store/uiSlice.ts:
```typescript
export interface UiSlice {
  selectedNode: ForceNode | null; isPanelOpen: boolean; expandingMbid: string | null;
  reheatCounter: number; selectNode; triggerReheat;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ControlPanel component, ShortcutOverlay, uiSlice extensions, and App wiring</name>
  <files>
    packages/viewer/src/components/ControlPanel.tsx,
    packages/viewer/src/components/ControlPanel.css,
    packages/viewer/src/components/ShortcutOverlay.tsx,
    packages/viewer/src/components/ShortcutOverlay.css,
    packages/viewer/src/store/uiSlice.ts,
    packages/viewer/src/App.tsx,
    packages/viewer/src/index.css
  </files>
  <read_first>
    packages/viewer/src/components/DetailPanel.tsx,
    packages/viewer/src/components/DetailPanel.css,
    packages/viewer/src/store/uiSlice.ts,
    packages/viewer/src/store/controlSlice.ts,
    packages/viewer/src/store/index.ts,
    packages/viewer/src/store/graphSlice.ts,
    packages/viewer/src/App.tsx,
    packages/viewer/src/index.css,
    packages/viewer/src/utils/exportJson.ts,
    packages/viewer/src/utils/exportGexf.ts,
    .planning/phases/04-controls-export-and-polish/04-UI-SPEC.md
  </read_first>
  <action>
1. **Extend uiSlice** (`packages/viewer/src/store/uiSlice.ts`):
   - Add `focusedNodeMbid: string | null` (default null)
   - Add `isShortcutOverlayOpen: boolean` (default false)
   - Add `setFocusedNode: (mbid: string | null) => void`
   - Add `toggleShortcutOverlay: () => void`
   - Add to UiSlice interface and createUiSlice implementation

2. **Add CSS custom properties** to `packages/viewer/src/index.css` `:root`:
   ```css
   --color-status-active: #22c55e;
   --color-status-warning: #eab308;
   --color-status-error: #ef4444;
   --color-status-disabled: #4a4a4a;
   --color-focus-ring: rgba(59, 130, 246, 0.5);
   ```

3. **Create ControlPanel.tsx** at `packages/viewer/src/components/ControlPanel.tsx`:
   - Mirror DetailPanel slide pattern: `position: absolute; left: 0; top: 0; z-index: 20`
   - Two states: expanded (220px) and collapsed (40px icon strip)
   - Use `useGraphStore` with `useShallow` for all state (following established pattern)
   - Auto-collapse: `useRef` timer, `onMouseMove`/`onMouseEnter` reset timer, `onMouseLeave` starts 3000ms collapse. Don't collapse if an input/range is focused inside sidebar (check `document.activeElement` is inside sidebarRef)
   - Re-open: `onMouseEnter` on collapsed strip or click on strip

   **Collapsed icon strip**: 40px wide, vertical stack of 3 inline SVG icons (sliders, grid, download) at 20px size, color `var(--color-text-secondary)`, hover `var(--color-text-primary)`, gap 16px

   **Expanded sections** (each with 12px/400 section heading in `var(--color-text-secondary)`):

   a. **"Controls" section**:
      - Depth slider: `<input type="range" min={1} max={5} step={1} value={maxDepth}>` with label "Depth" left-aligned 12px, value right-aligned 14px/600. Height 44px. `onChange` calls `setMaxDepth(Number(e.target.value))`
      - Node limit slider: `<input type="range" min={10} max={200} step={10} value={nodeLimit}>` with label "Max Nodes". `onChange` calls `setNodeLimit(Number(e.target.value))`

   b. **"Providers" section**:
      - One row per provider in order: MusicBrainz, ListenBrainz, Last.fm, Deezer, TasteDive, Spotify
      - Each row (36px height): [8px status dot] [14px provider name] [36x20 toggle switch]
      - Status dot color: `--color-status-active` if providerStatus is 'active', `--color-status-warning` if 'rate-limited', `--color-status-error` if 'erroring', `--color-status-disabled` if not in enabledProviders
      - Fetch animation: when `providerIsFetching[id]` is true, status dot gets CSS class `status-dot--fetching` with `@keyframes pulse-dot { 0%,100% { opacity: 1 } 50% { opacity: 0.3 } }` at 800ms loop
      - Rate limit display: below name, 12px/400 `var(--color-text-muted)`, show if providerStatus is 'rate-limited'. Text: "Rate limited" + cooldown countdown if providerCooldownEndsAt is set (use `useEffect` with `setInterval` to tick down)
      - Toggle switch: 36x20px pill div with 16px white circle thumb. Off = `var(--color-surface)`, On = `var(--color-accent)`. Thumb translates 16px via `transform`. `onClick` calls `toggleProvider(id)`. Transition `transform 150ms ease`

   c. **"Layout" section**:
      - Two-segment toggle, 100% width, 32px height
      - Two buttons side by side: "Force" and "Radial"
      - Active segment: `var(--color-accent)` background, inactive: `var(--color-surface)`
      - `onClick` calls `setLayoutMode('force')` or `setLayoutMode('radial')`
      - 12px/400 labels, border-radius 8px outer, 4px inner, transition background 150ms ease

   d. **"Export" section**:
      - Two stacked buttons, gap 8px
      - "Export JSON" button: on click calls `buildJsonExport(nodes, links, seedMbid, seedName, maxDepth, [...enabledProviders])` then `downloadJson(data, seedName + '-graph.json')`. Disabled when nodes.length === 0
      - "Export GEXF" button: on click calls `buildGexf(nodes, links, seedName)` then `downloadGexf(xml, seedName + '-graph.gexf')`. Disabled when nodes.length === 0
      - Button style: `var(--color-surface)` bg, `1px solid var(--color-border)`, 14px/400, 36px height, 8px border-radius, hover `var(--color-surface-hover)`

   e. **"Reset Graph" button** at bottom:
      - 100% width, 36px height, transparent bg, `1px solid var(--color-border)`, text `var(--color-text-secondary)`, 14px/600, 8px border-radius
      - Hover: text `#ef4444`, border `#ef4444`
      - `onClick`: call `clearGraph()`, `resetControls()`, `selectNode(null)`, `history.replaceState(null, '', location.pathname)`

4. **Create ControlPanel.css** at `packages/viewer/src/components/ControlPanel.css`:
   - `.control-panel`: position absolute, left 0, top 0, height 100%, z-index 20, background `var(--color-bg)`, border-right `1px solid var(--color-border)`, transition `width 200ms ease-in-out`, overflow hidden
   - `.control-panel--expanded`: width 220px, padding 32px 24px (using 16px inline for sections)
   - `.control-panel--collapsed`: width 40px
   - All internal elements: proper sizing per UI-SPEC
   - Slider styles: track 4px height `var(--color-surface)`, thumb 16px circle `var(--color-accent)` with box-shadow `0 0 0 4px rgba(59,130,246,0.2)`. Use `-webkit-appearance: none` and vendor prefixes for range styling
   - `@keyframes pulse-dot { 0%,100% { opacity: 1 } 50% { opacity: 0.3 } }`
   - `.status-dot--fetching { animation: pulse-dot 800ms ease-in-out infinite; }`

5. **Create ShortcutOverlay.tsx** at `packages/viewer/src/components/ShortcutOverlay.tsx`:
   - Renders when `isShortcutOverlayOpen` is true (from uiSlice)
   - Backdrop: `rgba(0, 0, 0, 0.6)` fullscreen overlay, z-index 90, onClick dismisses
   - Card: 360px width, centered, z-index 100, `var(--color-bg)` bg, `1px solid var(--color-border)`, border-radius 12px, padding 24px
   - Title: "Keyboard Shortcuts" 20px/600
   - Two-column layout: key badge left, description right
   - Key badge: `var(--color-surface)` bg, `1px solid var(--color-border)`, 12px monospace, padding 4px 8px, border-radius 4px
   - Description: 14px/400 `var(--color-text-primary)`
   - Row gap 8px
   - Shortcut list: Tab/"Next node (by similarity)", Shift+Tab/"Previous node", Arrow keys/"Navigate to nearest node", Enter/"Expand focused node", Escape/"Deselect / close panel", / /"Focus search", ? /"Toggle this overlay"

6. **Create ShortcutOverlay.css** at `packages/viewer/src/components/ShortcutOverlay.css`

7. **Update App.tsx**:
   - Import ControlPanel and ShortcutOverlay
   - Add `<ControlPanel />` before `<GraphCanvas />` in JSX
   - Add `<ShortcutOverlay />` after `<DetailPanel />`
   - Import and call `useUrlState()` in App component body (after initEngine)
  </action>
  <verify>
    <automated>cd /Users/billyendson/Games/VibeCoding/similar-artists-graph && pnpm --filter @similar-artists-graph/viewer tsc --noEmit && pnpm --filter @similar-artists-graph/viewer test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/viewer/src/components/ControlPanel.tsx contains `export function ControlPanel`
    - packages/viewer/src/components/ControlPanel.tsx contains `toggleProvider`
    - packages/viewer/src/components/ControlPanel.tsx contains `setMaxDepth`
    - packages/viewer/src/components/ControlPanel.tsx contains `setNodeLimit`
    - packages/viewer/src/components/ControlPanel.tsx contains `setLayoutMode`
    - packages/viewer/src/components/ControlPanel.tsx contains `downloadJson`
    - packages/viewer/src/components/ControlPanel.tsx contains `downloadGexf`
    - packages/viewer/src/components/ControlPanel.tsx contains `resetControls`
    - packages/viewer/src/components/ControlPanel.css contains `.control-panel`
    - packages/viewer/src/components/ControlPanel.css contains `pulse-dot`
    - packages/viewer/src/components/ShortcutOverlay.tsx contains `Keyboard Shortcuts`
    - packages/viewer/src/store/uiSlice.ts contains `focusedNodeMbid`
    - packages/viewer/src/store/uiSlice.ts contains `isShortcutOverlayOpen`
    - packages/viewer/src/App.tsx contains `<ControlPanel`
    - packages/viewer/src/App.tsx contains `<ShortcutOverlay`
    - packages/viewer/src/App.tsx contains `useUrlState`
    - packages/viewer/src/index.css contains `--color-status-active`
    - tsc --noEmit exits 0
    - all tests pass
  </acceptance_criteria>
  <done>ControlPanel sidebar renders with all controls, ShortcutOverlay shows on trigger, App wired with URL state and all new components</done>
</task>

<task type="auto">
  <name>Task 2: GraphCanvas extensions — filtering, radial layout, keyboard navigation, canvas resize, and provider status wiring</name>
  <files>
    packages/viewer/src/components/GraphCanvas.tsx,
    packages/viewer/src/components/GraphCanvas.css
  </files>
  <read_first>
    packages/viewer/src/components/GraphCanvas.tsx,
    packages/viewer/src/components/GraphCanvas.css,
    packages/viewer/src/store/controlSlice.ts,
    packages/viewer/src/store/uiSlice.ts,
    packages/viewer/src/store/graphSlice.ts,
    packages/viewer/src/utils/providerFilter.ts,
    packages/viewer/src/utils/keyboardNav.ts,
    .planning/phases/04-controls-export-and-polish/04-RESEARCH.md
  </read_first>
  <action>
1. **Fix SSR-unsafe window access** (PKG-04 prerequisite):
   - Line 85: Change `useState(window.innerWidth)` to `useState(() => typeof window !== 'undefined' ? window.innerWidth : 1200)`
   - Line 303: Change `height={window.innerHeight}` to use a state variable `windowHeight` (same SSR-safe pattern)

2. **Canvas width calculation** — extend for left sidebar:
   - Read `isSidebarExpanded` from controlSlice via `useGraphStore`
   - Change: `const leftSidebarWidth = isSidebarExpanded ? 220 : 40;`
   - Change: `const canvasWidth = windowWidth - leftSidebarWidth - (isPanelOpen ? 320 : 0);`
   - Only apply leftSidebarWidth when `seedMbid !== null` (sidebar only visible when graph exists)

3. **Apply client-side filtering** to graph data:
   - Read `maxDepth`, `nodeLimit`, `enabledProviders` from controlSlice
   - In the `graphData` useMemo, apply filters in order: `filterByProviders` -> `filterByDepth` -> `filterByNodeLimit`
   - Pass filtered result as `graphData` to ForceGraph2D
   - IMPORTANT per Pitfall 3: Do NOT pass a new filtered array to graphData if it causes layout restart. Instead use `nodeVisibility` callback prop if available, OR use `nodeCanvasObject` with `ctx.globalAlpha = 0` for hidden nodes. Test which approach works with the installed version of react-force-graph-2d. If nodeVisibility exists, use it. If not, filter in renderNode by setting alpha to 0 and returning early for hidden nodes, and filter links in renderLink similarly.
   - Store visible node mbids in a ref for use in renderNode/renderLink

4. **Radial layout toggle** (CTRL-06):
   - Read `layoutMode` from controlSlice
   - `useEffect` watching `layoutMode`:
     ```typescript
     if (layoutMode === 'radial') {
       const radialFn = forceRadial<ForceNode>(
         (node) => (node.depthFromSeed ?? 0) * 120,
         0, 0
       ).strength(0.8);
       graph.d3Force('radial', radialFn as unknown as Parameters<typeof graph.d3Force>[1]);
       graph.d3Force('charge', null);
       graph.d3ReheatSimulation();
     } else {
       graph.d3Force('radial', null);
       graph.d3Force('charge', forceManyBody().strength(-80) as unknown as Parameters<typeof graph.d3Force>[1]);
       graph.d3ReheatSimulation();
     }
     ```
   - Import `forceRadial` and `forceManyBody` from `d3-force`

5. **Keyboard navigation** (SHAR-02):
   - Read `focusedNodeMbid`, `setFocusedNode`, `toggleShortcutOverlay`, `isShortcutOverlayOpen` from uiSlice
   - Store `focusedNodeMbid` in a ref (same stale-closure pattern as other refs)
   - Add `useEffect` with `window.addEventListener('keydown', handleKeyDown)`:
     - Guard: if `document.activeElement?.tagName === 'INPUT'` return (don't intercept search typing)
     - `Tab` / `Shift+Tab`: cycle through nodes sorted by fusedScore edge to seed (highest first). Find current index, move +1/-1 with wrap. Call `setFocusedNode(next.mbid)`
     - Arrow keys: call `findNearestInDirection(focusedNode, nodes, e.key as Direction)`, set focus if found
     - `Enter`: if focusedNode exists, call `handleExpand(focusedNode)`
     - `Escape`: call `selectNode(null)`, `setFocusedNode(null)`, if `isShortcutOverlayOpen` call `toggleShortcutOverlay()`
     - `/`: `e.preventDefault()`, `document.querySelector<HTMLInputElement>('.search-input')?.focus()`
     - `?`: call `toggleShortcutOverlay()`
   - Use refs for nodes, links, focusedNodeMbid to avoid stale closures (same pattern as existing refs)

6. **Focus ring** in `renderNode`:
   - After existing rendering, check `if (node.mbid === focusedNodeMbidRef.current)`
   - Draw 2px ring at `radius + 4` with `strokeStyle = 'rgba(59, 130, 246, 0.5)'`
   - Only when keyboard navigation is active (focusedNodeMbid !== null)

7. **Auto-pan** on focus change:
   - `useEffect` watching `focusedNodeMbid`: find the node, call `graphRef.current?.centerAt(node.x, node.y, 300)` for 300ms smooth pan

8. **Provider status wiring** (STAT-01, STAT-02, STAT-03 — pragmatic fallback):
   - Since the Engine interface has no real-time event API, derive provider status reactively:
   - In the existing `handleExpand` callback, when `result.value.warnings` contains entries, call `setProviderStatus(warning.provider as ProviderId, 'erroring')` for each warning
   - Wire `providerIsFetching` state: before engine.expand call, set all enabled providers to fetching=true; in finally block, set all to fetching=false
   - This gives post-fetch status and fetch animation per STAT-01/02/03 (not real-time per-provider, but sufficient for the pragmatic fallback documented in research)
  </action>
  <verify>
    <automated>cd /Users/billyendson/Games/VibeCoding/similar-artists-graph && pnpm --filter @similar-artists-graph/viewer tsc --noEmit && pnpm --filter @similar-artists-graph/viewer test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/viewer/src/components/GraphCanvas.tsx contains `typeof window !== 'undefined'`
    - packages/viewer/src/components/GraphCanvas.tsx contains `leftSidebarWidth`
    - packages/viewer/src/components/GraphCanvas.tsx contains `filterByProviders` or `filterByDepth` or `nodeVisibility`
    - packages/viewer/src/components/GraphCanvas.tsx contains `forceRadial`
    - packages/viewer/src/components/GraphCanvas.tsx contains `findNearestInDirection`
    - packages/viewer/src/components/GraphCanvas.tsx contains `focusedNodeMbid`
    - packages/viewer/src/components/GraphCanvas.tsx contains `centerAt`
    - packages/viewer/src/components/GraphCanvas.tsx contains `setProviderStatus` or `providerIsFetching`
    - tsc --noEmit exits 0
    - all tests pass
  </acceptance_criteria>
  <done>GraphCanvas supports filtering, radial layout, keyboard navigation with focus ring and auto-pan, canvas resizes for sidebar, and provider status is wired</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete control panel with all Phase 4 features: collapsible sidebar with depth/node-limit sliders, provider toggles with status indicators, force/radial layout toggle, JSON and GEXF export buttons, reset button, keyboard navigation (Tab/arrows/Enter/Escape/slash/?), shortcut overlay, URL hash sharing, and canvas integration</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm --filter @similar-artists-graph/viewer dev`
    2. Open http://localhost:5173
    3. Search for "Radiohead" and select from autocomplete
    4. **Sidebar**: Hover left edge — sidebar should expand showing Controls, Providers, Layout, Export sections. Move mouse away — sidebar auto-collapses after ~3s
    5. **Depth slider**: Drag depth slider from 3 to 1 — nodes beyond depth 1 should disappear. Drag back to 3
    6. **Node limit**: Drag to lower value — least-similar nodes should disappear
    7. **Provider toggles**: Toggle off "ListenBrainz" — some nodes should disappear. Toggle back on — they return
    8. **Layout toggle**: Click "Radial" — nodes should arrange in concentric rings by depth. Click "Force" — returns to organic layout
    9. **Export**: Click "Export JSON" — file downloads. Click "Export GEXF" — file downloads. Open JSON to verify structure
    10. **Reset**: Click "Reset Graph" — everything clears, returns to empty search state
    11. **Keyboard**: Press Tab — a node gets focus ring. Arrow keys — focus moves spatially. Enter — node expands. Escape — deselects. / — search bar focused. ? — shortcut overlay appears
    12. **URL**: After exploring, check URL has `#seed=...&depth=...&providers=...`. Copy URL, open in new tab — same graph should load
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
TypeScript compiles without errors:
```bash
pnpm --filter @similar-artists-graph/viewer tsc --noEmit
```
All tests pass:
```bash
pnpm --filter @similar-artists-graph/viewer test --run
```
Dev server starts and renders:
```bash
pnpm --filter @similar-artists-graph/viewer dev
```
</verification>

<success_criteria>
- ControlPanel sidebar renders with all 5 sections (Controls, Providers, Layout, Export, Reset)
- Auto-collapse/expand works with 3s timeout
- Sliders control depth and node limit with instant visual feedback
- Provider toggles filter graph data without API re-fetch
- Layout toggle switches between force-directed and radial
- Export downloads working JSON and GEXF files
- Reset clears everything including URL hash
- Keyboard navigation works (Tab, arrows, Enter, Escape, /, ?)
- Focus ring visible on keyboard-focused node
- URL hash updates on state change and restores on load
- Graph canvas resizes properly for sidebar + detail panel
</success_criteria>

<output>
After completion, create `.planning/phases/04-controls-export-and-polish/04-02-SUMMARY.md`
</output>
